cargo build --message-format json --release | \
    jq -r '
        select(.reason == "compiler-artifact" and .target?.name? == "test-hello-world") |
        .filenames |
        map(select(test(".wasm$"))) |
        flatten[]'